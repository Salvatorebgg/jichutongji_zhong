# v91 极坐标与全局图形逻辑修复

本轮重点修复：
1. 极坐标贡献图增加坐标箭头
   - 新增径向箭头；
   - 新增角向（顺时针）方向箭头；
   - 不再只有圆网格，没有方向感。

2. 极坐标颜色按钮与图形一一对应
   - barpolar 改为按项目索引逐项着色；
   - 右侧颜色按钮与每一个扇区/变量一一对应；
   - 颜色调节后，图中对应扇区会同步变化，不再错位。

3. 背景与网格线功能修复
   - 白色背景 / 透明背景：对 polar 图同样生效；
   - 网格线显示 / 隐藏：对 radialaxis 与 angularaxis 同步生效；
   - 极坐标图不再出现“控件能选但图不变化”的情况。

4. 宽度联动增强
   - 极坐标柱宽支持跟随柱体宽度参数联动。

5. 自动巡检补充
   - 新增极坐标箭头、背景、网格、索引配色逻辑巡检。
