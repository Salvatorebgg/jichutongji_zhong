# v90 QDA 判别图逻辑修复

本轮修复：
1. 区分二次判别分析主图与“类别中心图”
   - 主图：保留正常类别散点分布；
   - 类别中心图：淡化原始点云，叠加更大的类别中心菱形标记，并加类别名称文本；
   - 对 QDA/LDA，这一页标题改为“类别中心定位图”，避免与主图视觉上过于相似。

2. 简化颜色调节逻辑
   - 类别中心辅助元素（中心点、中心引导线）不再单独占用颜色按钮；
   - 右侧颜色映射只保留真正需要用户理解和调节的主类别系列；
   - 中心点颜色自动继承所属类别颜色，避免“0 center / 1 center”一类混乱按钮。

3. 强化类别中心图层级
   - 原始散点在中心定位图中降低透明度；
   - 类别中心使用更大菱形标记；
   - 加入从原点指向中心的细虚线，帮助理解类中心方向。

4. 自动巡检
   - 新增 QDA/LDA 中心图与颜色逻辑巡检项。
